@model IEnumerable<DoBemSolution.Models.Curso>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeCurso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CargaHorariaCurso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Turma)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeCurso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CargaHorariaCurso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Turma.IdTurma)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCurso">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCurso">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCurso">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
